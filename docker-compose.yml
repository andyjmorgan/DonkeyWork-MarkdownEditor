version: '3.8'

services:
  markitdown-editor:
    image: ghcr.io/andyjmorgan/donkeywork-markdowneditor:latest
    container_name: donkeywork-markdown-editor
    ports:
      - "3000:80"
    restart: unless-stopped

networks:
  markitdown-network:
    driver: bridge
